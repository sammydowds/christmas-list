import { css } from '@emotion/react'

export const lightStrand = css`
    display: flex;
    flex-direction: row;
    overflow: hidden;
`

export const invertLights = css`
    transform: rotate(180deg);
`